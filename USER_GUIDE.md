# StockSentinel 使用指南（小白版）

> 写给懂股票但不懂量化的你
> 跟着步骤走，5 分钟跑起来

---

## 1. 这东西是啥？3 句话说清楚

1. **自选股管家**——你关注的股票，平台自动帮你盯着，跌多了就提醒你
2. **K 线 + 技术指标画图工具**——看一只股票的走势图，叠 MA / MACD / RSI 等
3. **量化回测沙盒**——"我如果用双均线策略买 5 只股票，过去 3 年能赚多少？" 让平台帮你算

**和东方财富 / 同花顺的区别**：你不用付费买数据，平台用免费数据源给你算；你写不出代码也能用菜单点点做回测。

---

## 2. 5 分钟跑起来

### 2.1 你需要装什么

| 工具 | 干什么 | 怎么装 |
|---|---|---|
| **Python 3.12** | 跑后端 | [python.org](https://www.python.org/downloads/) 装 3.12 |
| **Node.js 20+** | 跑前端 | [nodejs.org](https://nodejs.org) 装 LTS 版 |
| **uv** | 装 Python 包（比 pip 快 10 倍） | macOS: `brew install uv`；其他见 [astral.sh/uv](https://astral.sh/uv) |

### 2.2 三步启动

打开终端（macOS Terminal / Windows PowerShell），复制粘贴：

```bash
# 1. 进项目目录
cd ~/Documents/myCraft/stock-sentinel

# 2. 装依赖（首次运行需要 1-2 分钟）
uv venv .venv --python 3.12
uv pip install --python .venv/bin/python -r <(uv pip compile --quiet - <<EOF
fastapi "uvicorn[standard]" pydantic requests python-dotenv
pandas numpy scipy duckdb apscheduler akshare tushare pytest httpx
baostock
EOF
)

# 3. 一键启动（前后端一起）
./start.sh
```

看到这些就 OK 了：
```
============================================
  StockSentinel — 量化分析平台 v1.0
============================================

  Backend  : http://localhost:8000  (API)
  Frontend : http://localhost:5173  (UI)
```

**打开浏览器，访问 `http://localhost:5173/`**——看到 6 个页面的导航条就成功了。

### 2.3 端口被占了怎么办？

`./start.sh` 会自动避开（8000 → 8001，5173 → 5174）。如果 5 个端口都被占，报错会让你手动指定：

```bash
BACKEND_PORT=9000 ./start.sh
```

---

## 3. 6 大功能怎么用

页面导航条有 6 个 tab，下面挨个说。**先点哪个都行**，但建议按工作流顺序：

```
选股 → 回测 → 组合 → 风险
   ↑                ↓
  图表 (看走势)
```

### 3.1 📊 Dashboard（自选股监控）—— 一眼看完你的持仓

**这是什么**：你关注的所有股票，平台每 30 秒刷一次实时价，**跌到你的阈值**就在右上角弹通知。

**怎么用**：
1. 左上角输股票代码（`600519` / `AAPL` / `0700.HK`）+ 选市场 + 点 **添加**
2. 等几秒 → 列表出现该股票
3. 点股票右侧的 **⚙ 阈值** → 输 "跌 5% 提醒我" 之类的
4. 股价触发阈值 → 右上角弹红条

**提示**：你也可以点 **🌐 全局刷新** 一键刷所有自选股。

---

### 3.2 📈 Chart（K 线 + 技术指标）—— 看一只股票的走势

**这是什么**：输入代码，画出日 K / 周 K / 月 K + 成交量 + 各种技术指标。

**怎么用**：
1. 顶部输代码（`600519` = 贵州茅台，`AAPL` = 苹果，`00700` = 腾讯）
2. 选**周期**（日 K / 周 K / 月 K / 5 分钟 / 15 分钟 / 30 分钟 / 60 分钟）
3. 选**主图指标**（MA / EMA / BOLL / SAR ——这些画在 K 线图上）
4. 选**下方振荡器**（MACD / RSI / KDJ ——画在 K 线下面的小图）
5. 鼠标移到 K 线上 → 看十字光标的 OHLC（开高低收） + 当时所有指标的数值

**例子**：看贵州茅台 + 双均线：
- 代码 `600519`，周期 `1d`
- 主图指标 `MA`（默认就是 5 / 10 / 20 / 60 日均线，画在 K 线上）
- 鼠标移到 K 线上 → 看均线缠绕 / 交叉 → 决定买卖

**注意**：**5 分钟 K 线 / 15 分钟 K 线** 来自东方财富；**日 K / 周 K / 月 K** 来自 BaoStock。如果画不出来，多半是数据源限流，等几秒刷新。

---

### 3.3 🔍 Screener（多因子选股）—— 找"低 PE 高 ROE 的银行股"

**这是什么**：用 15 个因子从全 A 股（5500+ 只）筛出符合你条件 + 排名最高的 N 只。

**怎么用**：
1. 第一次用先点 **🔄 刷新因子库**（拉全 A 股 + 算因子 + 入库，**首次需 10-30 秒**）
2. 顶部**选行业**（可多选：银行 / 白酒 / 半导体 / 互联网 / ...；v1.1 后可用）
3. 列表里点因子按钮（如 `PE-TTM`）→ 自动加入"筛选条件"
4. 设范围（如 `PE-TTM 0 ~ 30`）
5. 选**排名方式**（如按 `ROE` 从高到低）
6. 设 **Top N**（默认 20）
7. 点 **执行选股** → 表格里就是你要的股票
8. 表格里点 **⚡ 回测** 按钮 → 跳到回测页面，**自动填好这些股票**！

**5 大类因子**（v1 共 15 个）：

| 大类 | 因子（举例） | 含义 |
|---|---|---|
| 估值 | PE-TTM / PB / PS-TTM | 越**低**越便宜（desc：降序=越低越靠前） |
| 成长 | ROE / ROA | 越**高**越赚钱 |
| 动量 | 20 日涨幅 / 60 日涨幅 | 越**高**越强势 |
| 波动 | 历史波动率 / ATR | 越**低**越稳 |
| 质量 | 毛利率 / 营收增速 | 越**高**越优质 |

**例子**：找 10 只"低 PE + 高 ROE + 不亏"的银行股
- 行业：勾 `银行`
- 筛选：`PE-TTM 0 ~ 10` + `PB 0 ~ 1.5` + `ROE 0.10 ~`
- 排名：ROE 降序
- Top N：10
- → 拿到 10 只"便宜又赚钱的银行股"

---

### 3.4 🔁 Backtest（回测）—— "过去 3 年用这个策略能赚多少？"

**这是什么**：用一个**策略**（如双均线、因子排名）在**历史 K 线**上模拟交易，输出**收益曲线 + 8 个关键指标**。

**怎么用**：
1. 选**策略**（4 个内置）：
   - **等权**：买入时平均分配资金
   - **双均线**：5 日均线上穿 20 日均线 → 买；下穿 → 卖
   - **因子排名**：按某个因子（动量 / 波动率）排名买入前 N
   - **固定权重**：按你给的权重买入（**组合回测必用**）
2. 输**标的代码**（逗号分隔，如 `600519,000001,300750`）
3. 选**起止日期**（默认 2020-01-01 ~ 今天）
4. 输**初始资金**（默认 100 万）
5. 点 **运行回测** → 后台跑（**30 秒 ~ 2 分钟**，取决于标的数和时间跨度）
6. 完成后看：
   - **8 个关键指标卡片**（总收益 / 年化 / 夏普 / 最大回撤 / 胜率 / ...）
   - **净值曲线**（你的钱 vs 基准）
   - **回撤曲线**（最高点跌了多少）
   - **交易记录表**（每笔买卖的时间 / 价格 / 数量）

**注意**：
- A 股有 **涨跌停**（主板 ±10%，创业板 / 科创板 ±20%），策略会处理
- 有**滑点**（默认 0.1%），模拟真实交易
- 资金不够买 100 股时，**跳过不买入**（不回测失败）

**例子**：测"双均线 + 贵州茅台 2020-2024"
- 策略：双均线（fast=5, slow=20）
- 代码：`600519`
- 日期：2020-01-01 ~ 2024-12-31
- 初始资金：100 万
- → 看到茅台在 5 年里双均线金叉 / 死叉几次，回报多少

---

### 3.5 💼 Portfolio（组合管理）—— "我有 100 万，60% 茅台 + 40% 宁德"

**这是什么**：建一个**多只股票**的组合，看实时估值 + 再平衡建议 + 一键组合回测。

**怎么用**：
1. 点 **+ 新建组合** → 输名字（如"价值股组合"）+ 选**基准**（沪深 300 / 中证 500 / 标普 500 / 恒生等）
2. 在组合页 → 点 **+ 加持仓** → 输代码 + 权重（如 600519 = 0.6, 300750 = 0.4）
3. **看估值**：平台拉实时价 × 目标权重 = 模拟总市值
4. **看再平衡建议**：当前权重 vs 目标权重 > 5% 偏差 → 提示"卖出 X / 买入 Y"
5. **一键回测**：按这个组合权重跑回测（用 fixed_weights 策略）

**注意**：v1 简化版**不考虑现金 + 持仓股数**（当前权重 = 目标权重）；v2 会升级。

**例子**：模拟"60% 茅台 + 40% 宁德"过去 3 年
- 组合：60% 贵州茅台 + 40% 宁德时代
- 基准：沪深 300
- → 一键回测，对比组合 vs 沪深 300 谁跑得好

---

### 3.6 ⚖️ Risk（风险分析）—— "我这个组合风险多大？"

**这是什么**：算**13 个风险指标**（夏普 / Sortino / 最大回撤 / 波动率 / VaR / Beta / Alpha / ...），画**净值曲线 + 回撤曲线**。

**怎么用**：
1. **方式 A**：从回测历史选一个已完成的回测 → 自动加载
2. **方式 B**：手输 equity_curve JSON（`[{"date": "2024-01-01", "value": 1000000}, ...]`）
3. 点 **计算** → 看：
   - **13 风险指标**（分 3 组：收益 / 风险 / 相对）
   - **净值曲线**（你的钱随时间变化）
   - **回撤曲线**（从最高点跌了多少 %）

**13 指标速查**：
- **收益组**：总收益 / 年化收益 / 年化波动率
- **风险组**：夏普 / Sortino / Calmar / 最大回撤 / VaR(95%) / CVaR(95%)
- **相对组**（vs 基准）：Alpha / Beta / 信息比率 / 跟踪误差

**例子**：跑完一个回测 → 跳到风险页 → 看夏普值（> 1 算不错，> 2 算优秀）+ 最大回撤（< 20% 算控制得好）

---

## 4. 数据源说明（哪些免费，哪些要 token）

平台**纯免费**起步，但有些数据源要 API key / token / 积分：

| 数据源 | 干什么 | 限速 / 配置 |
|---|---|---|
| **东方财富** | A 股 / 港股 K 线 | **免费但限速**（一分钟几次），不稳定 |
| **AkShare** | A 股 K 线 / 因子 | **免费但限速**，spot 接口经常超时 |
| **BaoStock** | A 股 K 线 + 行业 + 财务 | **免费无积分**！v1.1 后是主要因子源 |
| **Finnhub** | 美股 K 线 | **免费档**，需 [注册](https://finnhub.io) 拿 API key（没 key 时美股返回空） |
| **Tushare** | 因子 / 财务 | 需 [注册](https://tushare.pro) 拿 token + **200 积分**才能用 daily_basic |

### 配置可选 token（让数据更全）

```bash
# 加到 ~/.zshrc（macOS）或 ~/.bashrc（Linux），重启终端
export FINNHUB_API_KEY="你的_key"
export TUSHARE_TOKEN="你的_token"
```

**没配置**也能用——平台自动 fallback 到 Mock 数据（5000+ 只"看似真实"的股票，含行业 / PE / PB / 换手率 / 市值）。回测用 Mock 数据也是有效的（虽然不是真实价）。

### 限流了怎么办？

如果你看到 **K 线拉不到** / **选股器返回 0 只** / **行业字段全空**：
- 99% 是**数据源限流**（不是 bug）
- 等 5-10 分钟再点 **🔄 刷新**
- 平台会**自动 fallback** 到下一个源，**不报错**

---

## 5. 5 个常见问题

### Q1：刷新因子库失败 / 选股器空？

99% 是数据源限流。等几分钟后重试。如果一直失败：
- 检查后端控制台有没有 error
- 配置 `TUSHARE_TOKEN`（200 积分能解锁 daily_basic）
- 或用 Mock 数据（自动 fallback，不需要配置）

### Q2：K 线图出来但很多天是空的？

**东方财富**对单 IP 频率敏感。**等 30 秒** + 点页面刷新按钮。多次失败 → 看后端 log。

### Q3：回测跑了 5 分钟还没完？

标的数 × 时间跨度 = 任务量。**5 只 × 1 年 ≈ 10 秒**，**50 只 × 5 年 ≈ 1-2 分钟**。再多就慢。可以后台跑、关页面，过会回来看。

### Q4：组合权重超过 100% 或负数？

**v1 简化版不自动校验**。输权重时自己保证：
- 总和 = 1.0 (100%)
- 每个权重 ≥ 0
- 未来 v2 会自动归一化

### Q5：能实盘交易吗？

**不能**。v1 是**纯研究 / 回测平台**，不接券商 API、不下单。Tier 3 才会做（详见 `docs/quant-roadmap.md`）。

---

## 6. 想要更多功能？

平台是**个人项目**（GitHub: `CookieJobs/stock-sentinel`），按 4 个 Tier 推进：

| Tier | 功能 | 状态 |
|---|---|---|
| 1（v1.0） | 监控 + K 线 + 选股 + 回测 + 组合 + 风险 | ✅ 已完成 |
| 2 | 策略模板 + 模拟盘 + 事件日历 + AI 辅助 | 🚧 进行中 |
| 3 | 实盘交易 + 社区 + Level-2 行情 | 📅 未来 |
| 4 | 自然语言投研 + 智能资产配置 | 📅 远期 |

详细路线图见 [`docs/quant-roadmap.md`](docs/quant-roadmap.md)。

---

## 7. 关掉 / 重启 / 出问题

### 正常关掉
```bash
pkill -f "stock-sentinel.*main.py"
pkill -f "stock-sentinel.*vite"
```

或者 `Ctrl+C` 杀掉 `./start.sh` 启动的进程。

### 重新启动
```bash
cd ~/Documents/myCraft/stock-sentinel
./start.sh
```

### 端口冲突
```bash
BACKEND_PORT=9000 ./start.sh
```

### 数据库清空（重置所有数据）
```bash
rm data/sentinel.db
./start.sh  # 启动时自动重建
```

### 完全重装
```bash
rm -rf .venv
uv venv .venv --python 3.12
uv pip install --python .venv/bin/python -r <(uv pip compile --quiet - <<EOF
fastapi "uvicorn[standard]" pydantic requests python-dotenv
pandas numpy scipy duckdb apscheduler akshare tushare pytest httpx
baostock
EOF
)
```

---

## 8. 给小白的一句话总结

> **这个平台 = 你自己的免费聚宽 / 米筐。**
> 加自选股看实时价，画 K 线看走势，跑回测看策略能不能赚钱。
> 不用写代码也能用，写代码能改策略。
> 数据全免费，**多等 5-10 分钟** 限流就解了。
> 出问题先看 `/tmp/sentinel-backend.log` 末尾。

有 bug 或想新功能？去 GitHub 提 issue：
👉 https://github.com/CookieJobs/stock-sentinel/issues

---

*文档版本：v1.0（2026-06-07）*
